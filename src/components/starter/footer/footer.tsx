import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <footer
      style={{ width: "100%", border: "1px solid white", height: "50px" }}
    >
      Footer
    </footer>
  );
});
